# VLM 概述 (Vision-Language Model)

视觉语言模型，让 LLM 能"看懂"图像并用自然语言进行**开放式**视觉理解。

## VLM 到底在解决什么问题？

### 不只是"看图识字"

VLM 解决的核心问题是：**用自然语言进行开放式的视觉理解和推理**。

"看图识字"（OCR/图像分类）只是 VLM 能力的冰山一角。VLM 真正强大之处在于：

| 能力 | 示例 | 传统 CV 能做吗？ |
|------|------|-----------------|
| 图像描述 | "描述这张图" → 生成一段话 | 不能（只能输出类别/框） |
| 视觉问答 | "图中的人在做什么？" → "他在遛狗" | 不能（没有语言理解） |
| 视觉推理 | "这个场景可能发生在什么季节？为什么？" | 完全不能 |
| OCR + 理解 | 拍一张菜单 → "有哪些素食选项？" | OCR 能识字，但不能理解语义 |
| 文档理解 | 分析表格/图表/论文截图 | 不能 |
| 代码生成 | 看 UI 截图 → 生成前端代码 | 不能 |
| 多图推理 | 比较两张图的差异 | 不能 |
| 视觉定位 | "找到图中的红色汽车" → 输出坐标 | 目标检测能做，但不能自然语言交互 |

### 与传统 CV 的本质区别

```
传统 CV (封闭式):
  输入: 图像
  输出: 固定类别 / 边界框 / 分割掩码
  范式: 训练什么就只能做什么，每个任务一个模型

  ImageNet 分类器: 图像 → "金毛犬" (1000 个类别选一个)
  YOLO 目标检测:   图像 → [类别+框, 类别+框, ...]
  Mask R-CNN:      图像 → [类别+框+掩码, ...]

VLM (开放式):
  输入: 图像 + 任意自然语言问题
  输出: 自然语言回答（可以包含坐标、代码、推理过程等）
  范式: 一个模型解决所有视觉理解任务

  VLM: 图像 + "这是什么品种的狗？" → "这是一只金毛寻回犬，大约2岁..."
  VLM: 图像 + "图中有几个人？" → "3个人，两个站着一个坐着"
  VLM: 图像 + "写一首关于这个场景的诗" → "..."
```

### 范式转变的关键

| 维度 | 传统 CV | VLM |
|------|---------|-----|
| **任务定义** | 预先定义好（分类/检测/分割） | 用自然语言任意定义 |
| **输出形式** | 结构化（类别ID、坐标） | 自然语言（可以是任何内容） |
| **类别集合** | 封闭集（训练时固定） | 开放集（任意类别） |
| **交互方式** | 无（输入图像→输出结果） | 多轮对话 |
| **推理能力** | 无 | 有（继承 LLM 的推理能力） |
| **世界知识** | 无 | 有（LLM 预训练的知识） |
| **一个模型多任务** | 不行（每个任务单独训练） | 可以（统一模型） |

> **一句话总结**：传统 CV 是"看到什么说什么"（模式识别），VLM 是"理解看到的东西并能讨论它"（认知理解）。

---

## 核心架构：三大组件

LLM 只能处理文本 token，如何让它理解图像？

**答案：把图像编码成一组"视觉 token"，和文本 token 一起送入 LLM。**

![LLaVA 架构: Vision Encoder + 投影模块 + LLM (Liu et al., 2023)](assets/llava_architecture.png)

> 图源: *Visual Instruction Tuning (LLaVA)*, Figure 1. 图像经 Vision Encoder 编码为视觉特征，经投影模块对齐维度后，与文本 token 一起送入 LLM 统一处理。

VLM 的通用架构由三大组件构成：

```
图像 → [Vision Encoder] → 视觉特征 → [投影模块] → 视觉 token
                                                        ↓
                                    文本 token → [  LLM  ] → 回答
```

| 组件 | 作用 | 常见选择 |
|------|------|---------|
| **Vision Encoder** | 将图像编码为特征向量序列 | CLIP ViT、SigLIP、DINOv2、InternViT |
| **投影模块** | 将视觉特征映射到 LLM 输入空间 | MLP、Q-Former、Perceiver Resampler |
| **LLM** | 联合理解视觉+文本，生成回答 | LLaMA、Qwen、InternLM、GPT-4 |

> 详见 → [VLM 三大组件详解](VLM三大组件详解.md)

---

## 主流 VLM 模型速览

| 模型 | 团队 | Vision Encoder | LLM | 特点 |
|------|------|---------------|-----|------|
| **GPT-4o** | OpenAI | 未公开 | GPT-4 | 原生多模态，图文音视频统一 |
| **Claude 3.5** | Anthropic | 未公开 | Claude | 图像理解能力强 |
| **Gemini 2.0** | Google | 未公开 | Gemini | 原生多模态 |
| **Qwen-VL2** | 阿里 | ViT (自训) | Qwen2 | 动态分辨率，多语言 |
| **InternVL2.5** | 上海AI Lab | InternViT-6B | InternLM2 | 开源最强之一 |
| **LLaVA-OneVision** | 社区 | SigLIP | Qwen2 | 统一图像/视频/多图 |
| **DeepSeek-VL2** | DeepSeek | SigLIP | DeepSeek-MoE | MoE 架构，高效推理 |
| **Cambrian-1** | NYU | 多 Encoder 融合 | LLaMA-3 | 探索最优视觉表征组合 |

> 详见 → [VLM 主流架构详解](VLM主流架构详解.md)

---

## 深入阅读

本系列文章从不同角度深入讲解 VLM：

| 文章 | 内容 |
|------|------|
| [VLM 三大组件详解](VLM三大组件详解.md) | Vision Encoder、投影模块、LLM 骨干的深入分析 |
| [VLM 主流架构详解](VLM主流架构详解.md) | LLaVA、BLIP-2、InternVL2、Qwen-VL 等具体架构 |
| [VLM 关键技术](VLM关键技术.md) | 高分辨率处理、视觉 Token 压缩、多图/视频理解 |
| [VLM 训练与评测](VLM训练与评测.md) | 三阶段训练流程、数据构造、评测基准与榜单 |

**相关文档**：
- [对比学习与CLIP详解](../../视觉/表示学习/对比学习与CLIP详解.md) — CLIP 作为 VLM 视觉编码器的原理
- [DINO详解](../../视觉/表示学习/DINO详解.md) — DINOv2 特征在 VLM 中的应用

[返回上级](README.md) | [返回总目录](../../README.md)
