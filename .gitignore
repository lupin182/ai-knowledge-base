# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Environment
.env
!server/.env.example

# Runtime settings (contains API keys / passwords — never commit)
.settings.json
server/.settings.json
knowledge_bases/*/.kb/cache/

# Backup
_backup/
*.bak
_trash

# Accidental Windows env-var leak (don't touch — created by some external tool)
%SystemDrive%/

# CLI sandbox (auto-generated, now at parent dir)
_cli_sandbox/
debug_out.txt

# Uploaded-image scratch dir (server writes per-request, cleans up after)
.tmp_images/

# Claude Code
.claude/skills/idea-research-skill/

# OS
.DS_Store
Thumbs.db
desktop.ini

# IDE
.vscode/
.idea/
*.swp
*.swo
.auth_secret
